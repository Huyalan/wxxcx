<view class="section tc">
  <video id="myVideo"  class="video" style="height:{{scrollHeight}}px"
  src="{{movieList[id].src}}" danmu-list="{{danmuList}}" enable-danmu danmu-btn controls bindended="movieended"></video>
  <view class="btn-area">
    <input bindblur="bindInputBlur" bindfocus="bindInputFocus"/>
    <button bindtap="bindSendDanmu">发送弹幕</button>
  </view>
</view>
